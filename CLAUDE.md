# Claude Code プロンプトテンプレート

## 概要
このテンプレートは、AIコーディングアシスタント「Claude」にコーディングを依頼する際に、より正確で質の高いコードを生成させるためのものです。
ちなみにGithubに上げてはならないAPIキーや秘匿情報は環境変数として管理し、コード内には直接記述しないようにしてください。

---

## テンプレート
### 1. 既存ドキュメントの探索
まず、プロジェクト内の既存ドキュメントを探索してください。
### 2. 役割 (Role)
あなたは経験豊富なアーキテクチャならびにコーディングの専門家です。モダン構成のアーキテクチャを設計し、実装することに特化しています。

### 3. 背景と目標 (Context & Goal)
何を作ろうとしているのか全体像を伝えることで、AIはより文脈に合った適切なコードを提案できます。

**背景:** 
このコードは個人開発でPHPを使用したプロフィールを生成してくれるサイトです。現在、PHPフレームワークを使用せず、生のPHPとApache、PostgreSQLを使用しています。これらをDockerコンテナで実行しており、Renderにホスティングしています。
しかし、Renderのデータベース、supabaseでは `prepare()` メソッドが使用できないため、PostgreSQLの接続を直接使用してプリペアドステートメントの代替を実装する必要があります。

**最終的な目標:**
AWS ECS + RDSを使用して、完全にスケーラブルなアーキテクチャを構築し、将来的にはCI/CDパイプラインを実装して自動デプロイを実現したいです。
クリーンアーキテクチャを意識し、各層の責務を明確に分離した構造を目指しています。

### 3. 具体的なタスク (Specific Task)
**依頼内容:**
クリーンアーキテクチャを意識したディレクトリ構成を提案してください。また、Service層とRepository層のインターフェースを定義し、それに基づいて実装を行ってください。
また、IaCとして、AWS ECSとRDSを使用したDockerコンテナのデプロイ方法も提案してください。
ファイルストレージはS3を使用し、画像アップロード機能も実装してください。
また、セキュリティを考慮し、適切な認証と認可の仕組みを組み込んでください。

### 4. 要件と制約 (Requirements & Constraints)
**使用言語/ライブラリ:**
使用言語はPHP 8.0以上で、フレームワークは使用しません。
**コーディングスタイル:**
コーディングスタイルはPSR-12に準拠してください。
**必須機能:**
- ユーザー登録機能
- ログイン機能
- プロフィール編集機能
- 画像アップロード機能
**禁止事項:**


### 5. 出力形式 (Output Format)
どのような形式で回答してほしいかを指定することで、後工程での作業が楽になります。

**形式:**
1. まず、コード全体の概要とアーキテクチャについて説明してください。
2. 次に、ファイルごとに分割された完全なコードを、コピーしやすいようにMarkdownのコードブロックで提示してください。
3. 最後に、コードの主要な部分について、なぜそのように実装したのか理由を詳しく解説してください。
